[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "uavlink-apt-bootstrap"
version = "1.0.0"
description = "Runs apt install before pip packages (internal bootstrap)"
